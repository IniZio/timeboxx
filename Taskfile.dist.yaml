version: '3'

output: prefixed

includes:
  website:
    dir: website
    taskfile: website

tasks:
  vendor:
    deps:
      - website:vendor

  dev:
    deps:
      - website:dev

  build:
    deps:
      - website:build