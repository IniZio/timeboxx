version: '3'

output: prefixed

includes:
  app:
    dir: app
    taskfile: app

  backend:
    dir: backend
    taskfile: backend

tasks:
  vendor:
    deps:
      - app:vendor
      - backend:vendor

  dev:
    deps:
      - app:dev
      - backend:dev

  build:
    deps:
      - app:build